W celu uruchomienia aplikacji lokalnie, należy użyć komend:

### `npm install`

a następnie:
### `npm start`

Aplikacja będzie dostępna na localhost [http://localhost:3000](http://localhost:3000) z poziomu przeglądarki.

